# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# env files
.env
.env.local
.env*.local

# misc
.DS_Store
*.pem
npm-debug.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma
/prisma/migrations/dev.db
.neon

# unrelated tool with its own git repo, vendored locally — not part of this project
/tools/OpenAlice/

# signal-engine's own venv/cache (it has its own .gitignore for the rest)
/tools/signal-engine/.venv/
/tools/signal-engine/.cache/
